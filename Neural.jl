# I'm going to build my own neural net from scratch because why not

"""
    train_neural(training_data, data_set; iterations, seed, verbose, graphic)

```
iterations = 100
seed = 1337
verbose = true
graphic = true
```
"""
function train_neural(
    training_data,
    layers;
    iteratations = 100,
    seed = 1337,
    verbose = true,
    graphic = true,
)



end # function
